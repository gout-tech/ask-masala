pipeline {
    agent any

    stages {
        stage('git checkout') {
            steps {
                git 'https://github.com/gout-tech/ask-masala'
            }
        }
        stage('Maven Build') {
            steps {
                sh "mvn clean package"
            }
        }   
    }
}
